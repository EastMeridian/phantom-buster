import { usePhantoms } from "../services/phantoms";

const PhantomScreen = () => {
  return (
    <div className="bg-background flex flex-col pt-12 min-h-screen px-8">
      salut
    </div>
  );
};

export default PhantomScreen;
